TASK: P1-T3 — FastAPI Entrypoint & E2E Route

═══════════════════════════════════════════════════════════════
OBJECTIVE
═══════════════════════════════════════════════════════════════
Create the FastAPI entrypoint and connect it to the OpenRouter client.
This will create a fully functional, E2E passthrough proxy for Phase 1.

═══════════════════════════════════════════════════════════════
CONSTRAINTS & RULES
═══════════════════════════════════════════════════════════════
- Target file: `smartrouter/main.py`
- Create a `POST` endpoint at `/v1/chat/completions`.
- The endpoint must accept `ChatCompletionRequest`.
- Instantiate the `OpenRouterClient` (from P1-T2) and use it to forward the request.
- Return the `ChatCompletionResponse` generated by the client.
- Do NOT use mock responses. This must be an E2E passthrough.

═══════════════════════════════════════════════════════════════
IMPLEMENTATION HINTS
═══════════════════════════════════════════════════════════════
- Instantiate the FastAPI app with title="SmartRouter" and version="1.0.0".
- Handle potential HTTP errors from the upstream provider and return appropriate FastAPI `HTTPException`s.

═══════════════════════════════════════════════════════════════
FILES LIST
═══════════════════════════════════════════════════════════════

FILES TO CREATE:
- smartrouter/main.py   ← FastAPI app entrypoint

Commit: "jules: implement fastapi entrypoint with e2e passthrough"
Target branch: feature/dev
